import { AccountGetters } from './getters';
import { AccountDefaultState, IAccountState } from './state';

describe('AccountGetters', () => {
  let testState: IAccountState;

  beforeEach(() => {
    testState = AccountDefaultState();
  });
});
